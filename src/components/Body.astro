---
export interface Props {
  className?: string
}

const { className = '' } = Astro.props as Props;
---

<body class={`bg-teal-50 dark:bg-zinc-900 text-zinc-900 dark:text-zinc-300 break-words leading-6 transition-colors duration-500 ${className}`}>
  <slot />
</body>
