---
export interface Props {
  className?: string;
}

const { className = '' } = Astro.props as Props;
---
<main role="main" class={`pt-10 mx-auto max-w-xs md:max-w-md ${className}`}>
  <slot/>
</main>

