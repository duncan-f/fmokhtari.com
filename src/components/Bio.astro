---
export interface Props {
  year: number;
}

const { year } = Astro.props as Props;
---
<div class="flex items-start justify-start gap-5 pt-3">
  <div class="font-bold">{year}</div>
  <div><slot /></div>
</div>

